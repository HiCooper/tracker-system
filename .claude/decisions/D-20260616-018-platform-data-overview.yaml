id: "D-20260616-018"
timestamp: "2026-06-16T20:30:00"
topic: "平台数据 overview(第二优先隐藏菜单)后端 + 前端入口/显示修正"
context: >
  隐藏菜单「平台数据」是分析模块门面(DAU/PV/实时/渠道/页面/留存/异常),页面+store+api 就绪缺后端。
chosen: >
  PlatformDataService 实现 7 个 GET 端点直查 ClickHouse;单位严格对齐前端(时长秒、率百分数/分数分项);
  前端恢复 /tracker/data-platform 路由+菜单;修正分析页两处显示 bug(day7Retention 误标为「跳出率」、
  写死的「跳出率(次数)」占位 → 改为「7 日留存」「人均会话数」)。
rejected:
  - "渠道新增uv/页面入口退出做精确 join: 需多次自连接代价高且 schema 不全;v1 近似(0)并标注"
  - "DAU/MAU 严格按 start/end: DAU 语义是单日;改为今日/近30天滚动口径"
  - "保留错误标签: 误导;直接修正为正确指标"
rationale: >
  以标准 CH 聚合一次补齐 overview 7 端点,复用身份缝合用户口径;同时修正前端误标显示,避免误读。
constraints_ref:
  - "docs-site/dev/roadmap.md(隐藏菜单优先级评审,第二优先)"
  - "constraints.yaml: api_backward_compat(新增端点)"
known_gaps:
  - "渠道新增uv、页面入口/退出为近似;DAU/MAU 为滚动口径;CH 端到端需验证"
impact:
  files:
    - "tracker-admin: controller/PlatformDataController, service/PlatformDataService, domain/dto/platform/PlatformDto"
    - "apps/admin: router.tsx, layouts/AdminLayout.tsx, pages/tracker/data-platform/PlatformDataPage(显示修正)"
  risk: "medium"
